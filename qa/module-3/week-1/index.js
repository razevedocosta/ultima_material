// Crie um algoritmo para efetuar a leitura de duas notas para um determinado aluno. 
// Realize o cálculo da média aritmética, exiba a nota obtida na tela e verifique 
// se a nota é maior ou menor que 6.

// ENTRADA
let idade = 71;

if ((idade >= 18) && (idade <= 70)) {
    console.log("Pode votar e o voto é obrigatório!");
} else if ((idade >= 16) && (idade < 18)) {
    console.log("O voto é opcional mas já pode votar!");
} else if (idade > 70) {
    console.log("Acima de 70 anos o voto é opcional e não precisa votar!")
}